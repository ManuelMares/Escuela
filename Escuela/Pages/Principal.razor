@page "/principal"
@using Escuela.Data
@using Escuela.Pages
Hola cómo estás
<div class="row sombraBox padding-1  expanded">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">

                    <div id="mySidenav" class="sidenav">
                        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
                        <a href="#" @onclick="@( e => ActualizarOpcion("Horario"))"><i class="fas fa-calendar-alt"></i> Horario</a>
                        <a href="#" @onclick="@( e => ActualizarOpcion("Clases"))"> <i class="fas fa-chalkboard-teacher"></i> Clases</a>
                        <a href="#" @onclick="@( e => ActualizarOpcion("Grupo"))"> <i class="fas fa-users"></i> Grupo</a>
                        <a href="#" @onclick="@( e => ActualizarOpcion("Alumnos"))"> <i class="fas fa-user-check"></i> Alumnos</a>
                    </div>
                    <a class="nav-link" href="#">Features</a>
                    <a class="nav-link" href="#">Pricing</a>
                    <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
                </div>
            </div>
        </div>
    </nav>
</div>

<span style="font-size:30px;cursor:pointer; color:black;" onclick="openNav()" > <i class="fas fa-bars"></i></span>





<div class="columns small-12 large-10 padding-1 radio-5">
    @if (opcion == "Clase")
    {<Maestro_VisorClase />;
    }
    @if (opcion == "Alumno")
    {<Maestro_VisorClase />;
    }
    @if (opcion == "Actividad")
    {<Maestro_VisorClase />;
    }
    @if (opcion == "Rescursos")
    {<Maestro_VisorRecursos />;
    }
</div>




@code{
    public string opcion = "Clase";

    private void ActualizarOpcion(string o)
    {
        opcion = o;
    }

}